/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  images:{
    domains:["static.vecteezy.com","img.freepik.com" ]
  }
};

export default nextConfig;
